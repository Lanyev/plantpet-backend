const categoriesControllers = require( './categories.controllers' )
const customersInfoControllers = require( './customersInfo.controllers' )
const customersControllers = require( './customers.controllers' )
const permissionsControllers = require( './permissions.controllers' )
const productsControllers = require( './products.controllers' )
const providersInfoControllers = require( './providersInfo.controllers' )
const providersControllers = require( './providers.controllers' )
const purchasesDetailsControllers = require( './purchasesDetail.controllers' )
const purchasesControllers = require( './purchases.controllers' )
const rolsControllers = require( './rols.controllers' )
const saleDetailsControllers = require( './salesDetail.controllers' )
const salesControllers = require( './sales.controllers' )
const usersControllers = require( './users.controllers' )


module.exports = {
    categoriesControllers,
    customersInfoControllers,
    customersControllers,
    permissionsControllers,
    productsControllers,
    providersInfoControllers,
    providersControllers,
    purchasesDetailsControllers,
    purchasesControllers,
    rolsControllers,
    saleDetailsControllers,
    salesControllers,
    usersControllers
}